package com.nexai.project.util;

import jakarta.servlet.http.HttpServletRequest;

public class RequestUtil {
    public static void processRequestErrors(HttpServletRequest request) {
//        String error = request.getParameter(ParameterName.ERROR);
//        String validation = request.getParameter(ParameterName.VALIDATION);
//
//        if (error != null) {
//            request.setAttribute(ParameterName.ERROR, error);
//        }
//        if (validation != null) {
//            request.setAttribute(ParameterName.VALIDATION, validation);
//        }
    }
}
